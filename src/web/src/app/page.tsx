import { Header } from '@/components/Header';

export default function LandingPage() {
  return (
    <main className="flex flex-col gap-2 h-screen bg-(--background)">
      <Header />
    </main>
  );
}
